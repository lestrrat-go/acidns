// Package examples is a collection of runnable examples demonstrating how
// to use github.com/lestrrat-go/acidns. Each example is an Example_<area>
// function in its own file (e.g. dnsclient_resolve_example_test.go ->
// Example_dnsclient_resolve), so contributors can add coverage without
// reading or modifying neighbouring examples.
//
// Run all examples with `go test ./examples/...`. Each example prints a
// deterministic OUTPUT comment that `go test` verifies.
package examples
